# ===================================
# Supabase PostgreSQL 配置
# ===================================
#
# 请按照以下步骤配置：
# 1. 复制此文件为 .env
# 2. 将下面的 DATABASE_URL 替换为你从Supabase获取的连接字符串
# 3. 保存文件
#
# 获取连接字符串：
# Supabase Dashboard → Settings → Database → Connection string (URI)
#

# 应用配置
NODE_ENV=development
PORT=3000
APP_URL=http://localhost:3000

# 数据库 - 粘贴你的Supabase连接字符串
DATABASE_URL=postgresql://postgres.xxxxxxxxxxxxx:SpoDa7qk1Y0DIfvU@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres

# JWT配置
JWT_SECRET=your-super-secret-key-change-this-in-production-$(openssl rand -hex 32)
JWT_EXPIRES_IN=7d
REFRESH_TOKEN_SECRET=your-refresh-token-secret-$(openssl rand -hex 32)
REFRESH_TOKEN_EXPIRES_IN=30d

# Redis (可选)
REDIS_URL=redis://localhost:6379

# 文件上传
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# 短信服务 (阿里云示例)
SMS_ACCESS_KEY=your_access_key
SMS_SECRET_KEY=your_secret_key
SMS_SIGN_NAME=your_sign_name
SMS_TEMPLATE_CODE=your_template_code

# 支付配置
# 微信支付
WECHAT_APP_ID=
WECHAT_APP_SECRET=
WECHAT_MCH_ID=
WECHAT_API_KEY=

# 支付宝
ALIPAY_APP_ID=
ALIPAY_PRIVATE_KEY=
ALIPAY_PUBLIC_KEY=

# 监控
SENTRY_DSN=
