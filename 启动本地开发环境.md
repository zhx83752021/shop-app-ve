# 本地开发环境启动指南

## ⚠️ 重要提示

**推荐直接访问线上部署版本**：

- 前端：https://shop.hybergy.cn/home
- 后端会自动连接 Vercel 部署的 API

如果必须本地开发，请按以下步骤操作：

## 步骤 1：配置后端环境变量

在 `backend` 目录创建 `.env` 文件（如果已存在则检查内容）：

```bash
# 数据库连接
DATABASE_URL="postgresql://postgres:SpoDa7qk1Y0DIfvU@db.xidbrgszgwrvqrpckjkw.supabase.co:5432/postgres"

# JWT 密钥
JWT_SECRET="your-secret-key-here-change-in-production"
JWT_REFRESH_SECRET="your-refresh-secret-key-here"

# 服务器配置
PORT=3001
NODE_ENV=development

# CORS 配置
CORS_ORIGIN="http://localhost:5173"
```

## 步骤 2：安装依赖并生成 Prisma Client

```bash
# 进入后端目录
cd backend

# 安装依赖
npm install

# 生成 Prisma Client
npm run prisma:generate

# 启动后端服务
npm run dev
```

## 步骤 3：启动前端

```bash
# 打开新的命令行窗口
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 启动前端服务
npm run dev
```

## 步骤 4：访问本地应用

- 前端地址：http://localhost:5173
- 后端 API：http://localhost:3001/api

## 🔧 常见问题

### 1. Prisma Client 错误

如果遇到 `Invalid prisma.xxx` 错误：

```bash
cd backend
npm run prisma:generate
```

### 2. 数据库连接失败

检查：

- `.env` 文件中的 `DATABASE_URL` 是否正确
- Supabase 数据库是否已执行完整建表脚本

### 3. 端口被占用

如果 3001 或 5173 端口被占用：

- 关闭占用端口的程序
- 或修改 `backend/.env` 中的 `PORT`
- 或修改 `frontend/vite.config.ts` 中的端口配置

## 💡 推荐做法

**对于生产环境测试，直接访问线上版本**：

- ✅ 无需本地配置
- ✅ 数据库已正确配置
- ✅ Prisma Client 已正确生成
- ✅ 环境变量已正确设置

访问：https://shop.hybergy.cn/home
